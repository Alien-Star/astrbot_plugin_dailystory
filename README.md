# 今日群聊小故事 · astrbot_plugin_dailystory

读取当天群聊消息记录，以提问的用户为主角，调用 AI 生成一段并不生动也不有趣的小故事。

<details>
  <summary><h1>折叠内容是生成的文案，说真的这很诡异😰</summary>
  
  ```
  # 《永不过期的牛来》

星海天罚举起那块发光的符石，整个冒险者酒馆的目光都被吸引了过来。

"助眠效果非常显著，"TA对酒馆老板说，"一打开封印，我就两眼一黑，内心无比平静，甚至听不到自己心跳了——眼前还闪过走马灯。"

老板吓得胡子直抖："那叫中毒！不叫助眠！"

但星海天罚毫不在意，因为TA今天的目标不是睡觉，而是寻找传说中的神器——《牛来》。

据说这本书能治愈一切悲伤。封面上刻着古老铭文："满足你的胃，它会治愈悲伤。"

TA翻开第一页，一道金光炸开。

"我的专属，很香吧。"一个声音从书里飘出来。

星海天罚定睛一看，是个自称"妈妈"的幽灵厨娘，正端着一锅永不过期的炖牛肉。旁边的冒险者Sheena丶万冬闻到香味，凑了过来："永不过期的来了——这能存进我的空间背包吗？"

小面包22从角落探出头："今天——"

"今天什么？"

"今天……先吃了再说。"她改口道。

就在众人争抢牛肉时，酒馆门被踢开。一位蒙面法师扬手甩出一道禁咒——一团密密麻麻的JSON代码如暴雨般倾泻而下。

"这又何必是DeepSeek！"星海天罚认出了那道法术的标记，"换个别家的Harness不行吗！"

法师冷笑："你账号已被禁用。"

"Your account has been disabled——"酒馆上空回荡着冰冷的系统提示音。

所有人沉默了。

然后莲华推门而入，看了看一片狼藉的酒馆，平静地说：

"中午好。"

就这么一句。风轻云淡。

星海天罚愣了两秒，突然笑了出来。TA合上《牛来》，把那锅永不过期的牛肉推到桌子中央。

"行吧，"TA说，"账号可以再注册，牛肉不能放凉。今天，先吃饱。"

众人围坐，幽灵厨娘欣慰地消散在光中。书页翻到最后一行——

*"这也是你的专属吗？"*

*"是大家的专属。"*

窗外，星海正亮，天罚未至。

一切刚刚好。

**——未完待续？也许明天群聊会写下新的一章。**
  ```
</details>

## 功能介绍

在 QQ 群里发送 `/今日故事`，插件会：

1. 通过 OneBot 协议端调用 `get_group_msg_history`，向历史方向翻页拉取**当天**（今天 00:00 起）的群聊消息。
2. 把消息整理成可读对话剧本（时间 + 昵称 + 文本，图片/语音等以占位符表示）。
3. 以**发起指令的用户**为主角，把群聊记录作为素材，调用 AstrBot 当前会话配置的 AI 模型生成一段小故事。
4. 直接在群里输出故事正文。

故事风格、长度、语言、读取消息条数上限均可在 WebUI 插件配置页调整。

## 指令

| 指令 | 别名 | 说明 |
|---|---|---|
| `/今日故事` | `/今日群聊故事`、`/群聊小故事`、`/今日小故事` | 读取当天群聊记录并生成以提问者为主角的小故事 |

> 仅在**群聊**中触发，私聊不响应。

## 配置项（WebUI 插件管理 → 今日群聊小故事）

| 配置项 | 说明 | 默认值 |
|---|---|---|
| `story_style` | 故事风格基调 | 轻松幽默的奇幻冒险 |
| `story_length` | 故事长度（传给模型的字数描述） | 300-600字 |
| `max_messages` | 读取消息条数上限（按页翻取，每页20条） | 80 |
| `story_language` | 生成故事使用的语言 | 中文 |

## 环境要求

- AstrBot >= 4.5.0
- 平台：仅支持 **aiocqhttp**（OneBot v11，如 NapCat / Lagrange 等）
- 协议端需支持 `get_group_msg_history` 接口
- 需在 AstrBot 后台配置好至少一个 AI 聊天模型（插件使用当前会话配置的模型，无需单独填 API Key）

## 安装

把 `astrbot_plugin_dailystory` 文件夹放入 `AstrBot/data/plugins/`，在 WebUI「插件管理」中重载插件即可。

## 目录结构

```
astrbot_plugin_dailystory/
├─ main.py              # 插件主代码
├─ metadata.yaml        # 插件元数据
├─ _conf_schema.json    # 可视化配置 Schema
├─ requirements.txt     # 无第三方依赖
├─ README.md            # 本文件
└─ .gitignore
```

## 说明

```
没有处理读取的内容只是读取固定条数，生成出来的内容有点抽象
后续可能添加特定用户和无意义数据过滤（不一定）
```
